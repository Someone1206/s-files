#pragma once
#include <Windows.h>
#include <string>

// Title Manager
// set the title of the specified window according to the
// format present in the preferences .txt

void SetTitToFormat(HWND hWnd);